# CLAUDE.md — 속초 와일드맵 (Sokcho WildMap)

> 이 문서는 Claude Code가 본 저장소에서 작업할 때 따라야 할 프로젝트 헌장(charter)입니다.
> 모든 구현 결정은 아래 인터뷰 합의 사항을 우선합니다.

---

## 1. 프로젝트 개요

**속초 와일드맵(Sokcho WildMap)** 은 설악산·속초 일대의 산림 야생동물 생태 정보를
실시간 GIS(공간정보)와 결합해 제공하는 **모바일 우선 웹앱**입니다.

- **슬로건:** "내 손안의 작은 설악산, 생명의 흔적을 따라가다"
- **핵심 가치:** 정확한 정보 제공(Data) · 양방향 소통(Connect) · 야생 생태 보호(Protect)
- **대상 사용자:** 속초 시민, 탐방객/관광객, 환경 단체, 속초시·국립공원공단 등 공공기관
- **출발점이 되는 메시지:** "기술은 인간뿐만 아니라 산양의 발걸음도 지켜야 합니다."
  (2024년 겨울 산양 1,022마리 폐사 → 디지털 대안 → 속초시 미래 가치)

---

## 2. 인터뷰로 확정된 핵심 결정 (Source of Truth)

| 항목 | 결정 사항 |
|------|-----------|
| **기술 스택** | Next.js + React + TypeScript (App Router) |
| **지도/GIS** | Leaflet + OpenStreetMap (무료, API 키 불필요) |
| **MVP 1순위 기능** | 야생동물 디지털 도감 |
| **초기 데이터** | 목업(Mock) JSON 데이터 — 실제 DB/API는 후속 단계 |
| **디자인 톤** | 자연친화·서정적 (설악의 숲·능선, 그린/어스 톤, 감성적) |
| **언어 / 반응형** | 한국어 전용, 모바일 우선(Mobile-first) 반응형 |

> ⚠️ 위 표와 충돌하는 구현은 하지 않습니다. 변경이 필요하면 먼저 사용자에게 확인합니다.

---

## 3. 기술 스택 & 컨벤션

- **프레임워크:** Next.js (App Router, `app/` 디렉터리)
- **언어:** TypeScript (strict 모드). `any` 지양, 명시적 타입 선언.
- **UI:** React 함수형 컴포넌트 + Hooks. 클래스 컴포넌트 금지.
- **스타일링:** Tailwind CSS (자연친화 톤의 커스텀 컬러 팔레트 정의).
- **지도:** Leaflet + react-leaflet, 타일은 OpenStreetMap.
  - 지도 컴포넌트는 SSR 비활성화(`dynamic import`, `ssr: false`)로 로드.
- **상태관리:** 우선 React 내장(useState/useContext)으로 시작. 복잡해지면 Zustand 검토.
- **데이터:** `/data` 또는 `/public/data`의 정적 JSON 목업. 타입은 `/types`에 정의.
- **패키지 매니저:** npm (lockfile 커밋).

### 코드 스타일
- 컴포넌트 파일: `PascalCase.tsx`, 유틸/훅: `camelCase.ts`.
- 사용자 노출 텍스트는 모두 **한국어**.
- 주석은 꼭 필요한 곳에만, 주변 코드 밀도에 맞춰 작성.
- 접근성(a11y): 이미지 `alt`, 버튼 `aria-label`, 시맨틱 태그 준수.

---

## 4. 디자인 가이드 (자연친화·서정적)

- **컬러:** 숲의 딥그린, 능선의 어스/모스 톤, 따뜻한 베이지/크림 배경.
  강조색은 산양·담비를 떠올리는 차분한 포인트 컬러.
- **타이포:** 가독성 높은 한글 고딕 + 헤드라인은 서정적 무게감.
- **무드:** 설악의 안개·기암절벽·숲의 정적. 과한 채도/네온 지양.
- **모바일 우선:** 한 손 조작, 큰 터치 타깃(최소 44px), 하단 내비게이션 고려.
- **이미지:** 실제 설악산 촬영 산양/알비노 담비 사진 활용 시 설득력 극대화 (목업 단계는 플레이스홀더).

---

## 5. 기능 로드맵

### MVP (1순위) — 야생동물 디지털 도감
- 대표종 카드 목록 + 상세 페이지 (이름, 멸종위기 등급, 특징, 서식지, 보호 메시지).
- **대표종 데이터(제안서 Page 9–10 기반):**
  - 산양 (멸종위기 I급) — 가파른 절벽 선호, 울타리 철거 후 이동 경로 복원 중인 생태 지표
  - 알비노 담비 (멸종위기 II급) — 3마리 협동 사냥, 사회성 강함
  - 반달가슴곰 (멸종위기 I급) — 복원 개체 유입 기대주
  - 수달 & 삵 — 계곡부/저지대 최상위 포식자, 로드킬·서식지 단절 이슈
  - 하늘다람쥐 & 까막딱따구리 — 건강한 숲의 지표종, 노거수 보호
  - 까치살모사 — 가을철 주의, 출몰 예상 지점 안내
- 종 검색/필터(등급·분류군), 모바일 카드 UI.

### 후속 단계 (제안서 기능, 추후 구현)
- **흔적 히트맵 지도** (Page 7): GPS 좌표 기반 야생동물 활동 히트맵.
- **시민 와일드 제보** (Page 8): 위치+사진 제보 → 지도 경고 핀.
- **실시간 뉴스–지도 매핑** (Page 6): 뉴스 텍스트 마이닝 → 위치 핀.
- 실제 DB(PostgreSQL/PostGIS) 및 공공데이터 API 연동, AI 종 식별.

> 각 기능은 목업 데이터로 UI/흐름을 먼저 완성한 뒤 실제 데이터로 교체하는 순서로 진행.

---

## 6. 권장 디렉터리 구조

```
/app                  # Next.js App Router 페이지/레이아웃
  /page.tsx           # 홈 (도감 진입)
  /guide              # 디지털 도감 (MVP)
    /page.tsx         # 종 목록
    /[id]/page.tsx    # 종 상세
/components           # 재사용 UI 컴포넌트
  /map                # Leaflet 지도 관련 (후속)
/data                 # 목업 JSON 데이터 (species.json 등)
/types                # TypeScript 타입 정의
/lib                  # 유틸/데이터 로더
/public               # 정적 자산, 동물 이미지(placeholder)
/styles               # Tailwind/글로벌 스타일
```

---

## 7. 개발 워크플로

- **작업 브랜치:** `claude/stoic-allen-zjN4E` (지정 브랜치 외 푸시 금지).
- **커밋:** 명확하고 서술적인 한국어 메시지. 기능 단위로 커밋.
- **푸시:** `git push -u origin claude/stoic-allen-zjN4E`. 네트워크 실패 시 지수 백오프 재시도.
- **PR:** 사용자가 명시적으로 요청할 때만 생성.
- **검증:** 코드 변경 후 `npm run build` / `npm run lint` 통과 확인 후 보고.

---

## 8. 작업 원칙 (Claude를 위한 지침)

1. 위 인터뷰 결정(§2)을 항상 우선한다. 임의로 스택/범위를 바꾸지 않는다.
2. **MVP(디지털 도감)부터** 완성하고, 후속 기능은 별도 단계로 진행한다.
3. 모든 데이터는 우선 **목업 JSON**으로 구성하고, 타입을 먼저 정의한다.
4. 모든 사용자 노출 텍스트는 **한국어**, UI는 **모바일 우선**으로 만든다.
5. 디자인은 **자연친화·서정적** 톤을 유지한다.
6. 불확실하거나 아키텍처에 큰 영향을 주는 결정은 사용자에게 먼저 확인한다.
